# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Origins (comma-separated)
# For production, add your frontend domain
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mosaicme

# Rate Limiting
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=3600

# JWT Authentication
# IMPORTANT: Change these values in production!
# Generate a secure secret key with: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-min-32-chars-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Analytics
ANALYTICS_ENABLED=true
# Generate a random salt with: openssl rand -hex 16
ANALYTICS_SALT=change-me-in-production-random-salt

# Initial Admin Setup
# Used by the create_admin.py script to create the first admin user
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=changeme123

# ============================================
# Production Deployment Checklist
# ============================================
# [ ] Change JWT_SECRET_KEY to a strong random value (min 32 chars)
# [ ] Change ANALYTICS_SALT to a random value
# [ ] Update CORS_ORIGINS to your production domain
# [ ] Change INITIAL_ADMIN_PASSWORD to a strong password
# [ ] Set up HTTPS/TLS
# [ ] Configure database backups
# [ ] Set up log monitoring
# [ ] Review and adjust rate limits
